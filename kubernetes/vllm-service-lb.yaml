apiVersion: v1
kind: Service
metadata:
  name: vllm-service-lb
  namespace: vllm
  labels:
    app: vllm
spec:
  type: LoadBalancer
  selector:
    app: vllm
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
